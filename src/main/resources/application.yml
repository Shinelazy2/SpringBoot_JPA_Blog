spring:  datasource:    driver-class-name: com.mysql.cj.jdbc.Driver    url: jdbc:mysql://localhost:3306/blog?serverTimezone=Asia/Seoul    username: root    password: ej2soft!@!  mvc:    view:      prefix: /WEB-INF/vies/      suffix: .jsp  jpa:    open-in-view: true    hibernate:      ddl-auto: create # create -> updeate 바꿔야함 null      naming:        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl # Hibernate , Class의 변수명을 그대로 쓰겠다는거임.      use-new-id-generator-mappings: false # JPA의 넘버링 전략을 따라가지 않겠다.    show-sql: true    properties:      hibernate.format_sql: true  jackson:    serialization:      fail-on-empty-beans: false